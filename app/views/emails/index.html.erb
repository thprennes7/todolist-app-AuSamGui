<div class="container mb-4">

  <div class="row mail-table">
    <div class="col-4 mail-list">
      <% @emails.each do |email| %>
      <h3 class="row pl-4 pr-4 title <%= email.id %>"><%= link_to "#{email.object}", email_path(email.id), remote: true %></h3>
      <% end %>
    </div>
    <div class="col-6 mail-reader">

    </div>
  </div>
</div>
